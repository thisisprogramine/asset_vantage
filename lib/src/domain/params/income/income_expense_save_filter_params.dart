class SaveIncExpFilterParams{
  final String? entityName;
  final String? grouping;
  final Object? filter;

  const SaveIncExpFilterParams({
    required this.entityName,
    required this.grouping,
    required this.filter,
  });
}